[package]
name = "topic_stream"
version = "0.1.0"
edition = "2021"

[dependencies]
dashmap = "6.1.0"
tokio = { version = "1.43.0", features = ["sync"] }
futures = "0.3.31"

[dev-dependencies]
tokio = { version = "1.43.0", features = ["macros", "rt-multi-thread", "time"] }
